# Phase 1 — Data Foundation

This phase will inspect, document, preprocess, and explore the IBM AML dataset.

The implementation will be added and validated incrementally. Do not run the phase scripts until the dataset files have been placed in `data/raw/` and the setup instructions have been completed.
